# P8 spot-check verdict (2026-07-05) — Meet speaking tokens ARE in AX post-wake

Live rig experiment (meeting aky-minm-qsh, host + 2 CDP fake-mic guests), product
binary from bubbles-dev (`--dump meet --attrs`, `--watch meet`). Raw captures: p8-*.txt.

## Findings
1. **Equalizer ladder tokens (Oaajhc/OgVli/HX2H7/wEsLMd) ARE in AXDOMClassList**
   after forceFullAXTree (AXManualAccessibility + AXEnhancedUserInterface).
   Silent = `DYfzY cYKTje gjg47c`; speaking tile drops gjg47c and carries a
   rotating ladder token (~250-500ms rotation), visible in AX. HANDOFF §"pruned
   from AX" is FALSIFIED. `data-audio-level` does NOT surface in AX (that claim stands).
2. **2-person calls have the full signal** — kssMZb is SPEECH-gated, not
   participant-count-gated: absent from all silent dumps (2p AND 3p), present on
   the speaking tile in both. The old "kssMZb absent for 2-person" finding
   sampled silent tiles.
3. **Not foreground-gated** on the rig (caveat: rig Chrome runs with
   --disable-backgrounding-occluded-windows etc.; production full-occlusion
   pause of the ladder animation remains untested → keep fallback tiers).
4. IisKdb bars-meter presence in AX varies run-to-run; match tokens on ANY tile
   node, never anchor on IisKdb. Ladder had a ~260ms all-absent dropout
   mid-rotation → 500ms-1s hold/debounce required. Self-tile meter partially
   pruned (host self-PIP had none in 2p) → self-speech stays mic-state/VAD.
5. Product diagnostic bug found: Watch/Dump pid fallback (`matched.isEmpty ?
   all : matched`) binds the first Chrome with ANY web area (fixed in Phase 3).

## Locked Phase 3 design (Architect)
Speaking = any tile node whose AXDOMClassList contains kssMZb OR a ladder token;
explicit-silent = gjg47c; kssMZb primary (steadier), ladder corroborating;
500ms-1s hold; NO foreground activation (forceActivateForCapture stays out);
structural + mic-state fallbacks retained.
